spring:
  datasource:
    url: jdbc:log4jdbc:mysql://database.c2krpnhphvoc.ap-northeast-2.rds.amazonaws.com:3306/db?characterEncoding=utf8&serverTimezone=UTC
    username: admin
    password: ytsrq0116!
    driver-class-name: net.sf.log4jdbc.sql.jdbcapi.DriverSpy
  jpa:
    hibernate:
      ddl-auto: update # 운영시에는 validate 추천
    show-sql: true
    generate-ddl: true

  error:
    whitelabel:
      enabled: falsed

logging:
  config: classpath:log4j2.xml
  level:
    root: info

mybatis:
  type-aliases-package: com.api.idus
  mapper-locations: classpath:mybatis/mapper/**/*.xml